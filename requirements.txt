# Uretildigi ortam: Windows 11, Python 3.12, .venv
#
# DIKKAT - torch CPU-only:
#   Asagidaki torch surumu PyPI'nin varsayilan (CPU) tekerlegidir. Bu ortamda
#   torch.cuda.is_available() == False, yani GTX 1650 Ti KULLANILMIYOR.
#   Olculen sureler (indeksleme ~2.5 saat, retrieval ~10 sn) CPU degerleridir.
#
#   GPU icin CUDA tekerlegini ayrica kur:
#     pip install torch --index-url https://download.pytorch.org/whl/cu121
#   Kurduktan sonra dogrula:
#     python -c "import torch; print(torch.cuda.is_available())"
#
#   4GB VRAM'de embedding + rerank + 7B LLM ayni anda sigmaz; app.py
#   embedding ve rerank'i bilerek CPU'ya sabitliyor (VRAM LLM'e kalsin).

chromadb==1.5.9
datasets==5.0.1
numpy==2.5.2
ollama==0.6.2
pdfplumber==0.11.10
requests==2.34.2
sentence-transformers==6.0.1
streamlit==1.63.0
torch==2.14.0
transformers==5.16.1
urllib3==2.7.0
